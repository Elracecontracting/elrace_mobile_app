# نظام التحقق النشط من الحيوية (Active Liveness Detection)

## 📋 نظرة عامة

تم تنفيذ نظام تحقق نشط متعدد المراحل لمنع التحايل باستخدام الصور أو الفيديوهات المسجلة.

## 🎯 التحديات الثلاثة

### التحدي الأول - الرمش (Blink Detection)

```
"يرجى إغلاق عينيك تمامًا ثم فتحهما مرة أخرى. يجب أن تكون الحركات سريعة وطبيعية."
```

- يتم اكتشاف تسلسل: عيون مفتوحة ← عيون مغلقة ← عيون مفتوحة
- عتبة الفتح: > 0.5
- عتبة الإغلاق: < 0.3

### التحدي الثاني - حركة الرأس (Head Movement)

```
"لف رأسك ببطء إلى اليمين ثم إلى اليسار، ثم انظر إلى الأعلى والأسفل."
```

- يتم اكتشاف تغير زاوية الرأس (Yaw/Pitch)
- الحد الأدنى للتغير: 15 درجة للف الرأس، 10 درجات للنظر أعلى/أسفل

### التحدي الثالث - حركة العين السريعة (Rapid Eye Movement)

```
"يرجى تحريك عينيك بسرعة بين اليمين واليسار."
```

- يتم اكتشاف تغير الاتجاه المتكرر
- يتطلب على الأقل 2 تغييرات في الاتجاه

## 🛡️ آليات الحماية

### 1. تحليل إطارات متعددة

- يتم جمع 15-20 إطار خلال 3-5 ثواني
- تحليل كل إطار لاستخراج:
  - احتمالية فتح العين (leftEyeOpenProbability, rightEyeOpenProbability)
  - زوايا الرأس (headEulerAngleX, headEulerAngleY, headEulerAngleZ)

### 2. مقارنة الحركات

- التأكد من أن الحركات تتغير بشكل طبيعي بين الإطارات
- اكتشاف الوجوه الثابتة (الصور) من خلال عدم وجود حركة طبيعية

### 3. تحليل متزامن

- التحقق من حركة العين والرأس في نفس الوقت
- التأكد من أن التفاعل يأتي من شخص حقيقي وليس من تحريك الهاتف

## 📁 الملفات المعدلة

### 1. LivenessService

```
lib/core/biometric/face_recognition/data/services/liveness_service.dart
```

- إضافة تحديات جديدة: `blinkEyes`, `rapidEyeMovement`
- إضافة `verifySingleChallenge()` للتحقق من تحدي واحد
- إضافة `verifyAllChallenges()` للتحقق من جميع التحديات
- إضافة `_detectRapidEyeMovement()` لاكتشاف حركة العين السريعة

### 2. Events

```
lib/core/biometric/face_recognition/presentation/bloc/face_recognition_event.dart
```

- إضافة `StartActiveLivenessVerification`
- إضافة `VerifySingleChallengeEvent`
- إضافة `RequestNextChallengeEvent`
- إضافة `ResetChallengesEvent`

### 3. States

```
lib/core/biometric/face_recognition/presentation/bloc/face_recognition_state.dart
```

- تحديث `LivenessChallengeInProgress` مع تعليمات وأيقونات
- إضافة `WaitingForNextChallenge`
- إضافة `SingleChallengeSuccess`
- إضافة `SingleChallengeFailed`

### 4. BLoC

```
lib/core/biometric/face_recognition/presentation/bloc/face_recognition_bloc.dart
```

- إضافة `_onActiveLivenessVerify()`
- إضافة `_onVerifySingleChallenge()`
- إضافة `_onRequestNextChallenge()`
- إضافة `_onResetChallenges()`

### 5. UI Screen

```
lib/core/biometric/face_recognition/presentation/screens/face_registration_screen.dart
```

- إضافة مؤشر تقدم التحديات
- إضافة بطاقة التحدي الحالي مع التعليمات
- إضافة رسائل الحالة بالعربية

## 🔄 تدفق العمل

```
1. المستخدم يفتح شاشة التحقق
   ↓
2. يتم تهيئة الكاميرا
   ↓
3. يبدأ جمع الإطارات (20 إطار ~ 4 ثواني)
   ↓
4. يتم عرض التحديات للمستخدم
   ↓
5. لكل تحدي:
   - عرض التعليمات
   - تحليل الإطارات
   - التحقق من اكتمال التحدي
   ↓
6. إذا نجحت جميع التحديات:
   - التحقق من مطابقة الوجه
   - إظهار النتيجة
   ↓
7. إذا فشل أي تحدي:
   - عرض رسالة الخطأ
   - السماح بالمحاولة مرة أخرى
```

## 🎨 واجهة المستخدم

### مؤشر التقدم

- دوائر مرقمة (1, 2, 3)
- تتحول إلى علامة ✓ عند الاكتمال
- لون أخضر للمكتمل، أزرق للحالي، رمادي للمنتظر

### بطاقة التحدي

- أيقونة التحدي
- عنوان التحدي (بالعربية)
- تعليمات مفصلة
- رسالة الحالة

## 📊 العتبات والإعدادات

| الإعداد          | القيمة  | الوصف                |
| ---------------- | ------- | -------------------- |
| عدد الإطارات     | 20      | الحد الأدنى للتحليل  |
| وقت الجمع        | 4 ثواني | المدة الزمنية        |
| عتبة فتح العين   | 0.5     | لاعتبار العين مفتوحة |
| عتبة إغلاق العين | 0.3     | لاعتبار العين مغلقة  |
| تغير زاوية الرأس | 15°     | للف يمين/يسار        |
| تغير النظرة      | 10°     | للنظر أعلى/أسفل      |
| تغيرات الاتجاه   | 2       | لحركة العين السريعة  |

## 🧪 الاختبار

للاختبار:

1. تشغيل التطبيق
2. الذهاب لشاشة التحقق من الوجه
3. اتباع التعليمات:
   - رمش العينين
   - لف الرأس
   - تحريك العينين
4. التأكد من ظهور رسالة النجاح

## ⚠️ ملاحظات مهمة

1. **الإضاءة**: يجب أن تكون الإضاءة جيدة لاكتشاف حركات العين بدقة
2. **المسافة**: يجب أن يكون الوجه واضحًا في الكاميرا
3. **السرعة**: الحركات يجب أن تكون طبيعية وليست سريعة جدًا
4. **الثبات**: لا تحرك الهاتف أثناء التحقق
